package com.catalis.core.users.interfaces.enums;

/**
 * Enum representing the types of users in the system.
 * Based on the user_type_enum in the database.
 */
public enum UserTypeEnum {
    /**
     * Represents an employee user.
     */
    EMPLOYEE,
    
    /**
     * Represents a distributor user.
     */
    DISTRIBUTOR
}